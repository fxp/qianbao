<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>零钱包抢钱</title>
    <meta name="viewport" content="width=device-width,initial-scale=1.0,maximum-scale=1.0,user-scalable=no"/>
    <meta http-equiv="cache-control" content="no-cache">
    <meta content="telephone=no" name="format-detection"/>
    <link rel="stylesheet" type="text/css" href="css/hongBao.css">
    <script src="http://res.wx.qq.com/open/js/jweixin-1.0.0.js"></script>
    <script>
        wx.config({
            debug: true, // 开启调试模式,调用的所有api的返回值会在客户端alert出来，若要查看传入的参数，可以在pc端打开，参数信息会通过log打出，仅在pc端时才会打印。
            appId: 'wx85447170f9e1db12', // 必填，公众号的唯一标识
            timestamp: '1422883491', // 必填，生成签名的时间戳
            nonceStr: '3txr53ql2q6nipb', // 必填，生成签名的随机串
            signature: 'da026872a6632f0ab2a8a8cdc017d8f6fc92f0a1',// 必填，签名，见附录1
            jsApiList: ['onMenuShareTimeline', 'showAllNonBaseMenuItem'] // 必填，需要使用的JS接口列表，所有JS接口列表见附录2
        });
    </script>
</head>
<body>
<div id='wx_logo' style='margin:0 auto;display:none;'>
    <img src='/images/0.jpg'/>
</div>
<div id="currentUrl">url</div>

<script>
    //    alert('before')
    document.getElementById('currentUrl').innerHTML = JSON.stringify(window.location)
    setTimeout(function () {
//        window.history.replaceState('Object', 'Title', '/kaka/');
        document.getElementById('currentUrl').innerHTML = JSON.stringify(window.location)
        wx.ready(function () {
//            wx.showAllNonBaseMenuItem();
            alert('onMenuShareTimeline')
            wx.onMenuShareTimeline({
                title: '啦啦啦啦', // 分享标题
                link: 'http://www.soohu.com', // 分享链接
                success: function () {
                    // 用户确认分享后执行的回调函数
                    alert('success')
                },
                cancel: function () {
                    // 用户取消分享后执行的回调函数
                    alert('cancel')
                }
            });
        });

    }, 3000)
</script>

</body>
</html>
